@using DataModel.DataViewModel
@using DataModel.DataEntity
@using DataModel.Extension;
@model IEnumerable<SubMenuViewModels>



@*@foreach (SubMenuViewModels _item in Model.CataList)
    {
        if (_item.lstChild.Count() > 0)
        {
            <li><a href="~/@_item.Parent.FriendlyURL"><i class="fa fa-chevron-right"></i> @_item.Parent.ClassificationNM</a></li>
        }
        else
        {
            <li><a href="~/@_item.Parent.FriendlyURL"><i class="fa fa-chevron-right"></i> @_item.Parent.ClassificationNM</a></li>
        }
    }*@

<div class="footer-v4">
    <div class="footer">
        <div class="container">
            <div class="row">
                <!-- About -->
                <div class="col-md-3 md-margin-bottom-40">

                    <a href="#"><img class="footer-logo img-responsive" src="~/Content/assets/img/logo-2.png" alt=""></a>


                    <!--<div class="col-md-9">
                        <p>
                            Với mong muốn phát triển thương hiệu sản phẩm bằng nội lực,
                            Đồ Gỗ Sồi đã chú trọng vào chất lượng và giá cả của sản phẩm.
                            Sản phẩm của Đồ Gỗ Sồi thường xuyên được nâng cao chất lượng
                            và cập nhật giá cả, liên tục cung cấp cho khách hàng các sản phẩm chất
                            lượng và rẻ nhất.Đồ Gỗ Sồi luôn phù hợp với mọi nhu cầu của khách hàng,
                            đem đến sự hài lòng tuyệt đối cho khách hàng. Sản phẩm được bán và giao hàng toàn quốc.
                        </p>
                    </div>-->


                    <ul class="list-unstyled address-list margin-bottom-20">
                        <li><i class="fa fa-angle-right"></i>A009 - Chung Cư CitiHome - Phường Cát Lái Quận 2 - TP.Hồ Chí Minh.</li>
                        <li><i class="fa fa-angle-right"></i>Phone: 0988824243</li>
                        <li><i class="fa fa-angle-right"></i>Email: phonglangiasi@gmail.com</li>
                    </ul>
                </div>


                <div class="col-md-3 col-sm-3">
                    <div class="row">
                        @foreach (SubMenuViewModels _item in Model)
                        {
                            if (_item.Parent.ClassificationId == (int)EnumCore.Classifi_Product_Cata.lan_quy_hiem)
                            {
                                <div class="col-sm-12 col-xs-6">
                                    <h2 class="thumb-headline">@_item.Parent.ClassificationNM</h2>
                                    <ul class="list-unstyled simple-list margin-bottom-20">
                                       @foreach (Classification _val in _item.lstChild)
                                       {
                                        <li><a href="~/@_item.Parent.FriendlyURL/@_val.FriendlyURL">@_val.ClassificationNM</a></li>
                                       }
                                    </ul>
                                </div>
                            }
                        }

                        @foreach (SubMenuViewModels _item in Model)
                        {
                            if (_item.Parent.ClassificationId == (int)EnumCore.Classifi_Product_Cata.ho_diep)
                            {
                                <div class="col-sm-12 col-xs-6">
                                    <h2 class="thumb-headline">@_item.Parent.ClassificationNM</h2>
                                    <ul class="list-unstyled simple-list margin-bottom-20">
                                        @foreach (Classification _val in _item.lstChild)
                                        {
                                            <li><a href="~/@_item.Parent.FriendlyURL/@_val.FriendlyURL">@_val.ClassificationNM</a></li>
                                        }
                                    </ul>
                                </div>
                            }
                        }
                    </div>
                </div>

                <div class="col-md-3 col-sm-3">
                    <div class="row">
                        @foreach (SubMenuViewModels _item in Model)
                        {
                            if (_item.Parent.ClassificationId == (int)EnumCore.Classifi_Product_Cata.cay_giong_chai_mo)
                            {
                                <div class="col-sm-12 col-xs-6">
                                    <h2 class="thumb-headline">@_item.Parent.ClassificationNM</h2>
                                    <ul class="list-unstyled simple-list margin-bottom-20">
                                        @foreach (Classification _val in _item.lstChild)
                                        {
                                            <li><a href="~/@_item.Parent.FriendlyURL/@_val.FriendlyURL">@_val.ClassificationNM</a></li>
                                        }
                                    </ul>
                                </div>
                            }
                        }

                        @foreach (SubMenuViewModels _item in Model)
                        {
                            if (_item.Parent.ClassificationId == (int)EnumCore.Classifi_Product_Cata.dendro)
                            {
                                <div class="col-sm-12 col-xs-6">
                                    <h2 class="thumb-headline">@_item.Parent.ClassificationNM</h2>
                                    <ul class="list-unstyled simple-list margin-bottom-20">
                                        @foreach (Classification _val in _item.lstChild)
                                        {
                                            <li><a href="~/@_item.Parent.FriendlyURL/@_val.FriendlyURL">@_val.ClassificationNM</a></li>
                                        }
                                    </ul>
                                </div>
                            }
                        }
                    </div>
                </div>

                <div class="col-md-3 col-sm-3">
                    <div class="row">

                        @foreach (SubMenuViewModels _item in Model)
                        {
                            if (_item.Parent.ClassificationId == (int)EnumCore.Classifi_Product_Cata.lan_rung)
                            {
                                <div class="col-sm-12 col-xs-6">
                                    <h2 class="thumb-headline">@_item.Parent.ClassificationNM</h2>
                                    <ul class="list-unstyled simple-list margin-bottom-20">
                                        @foreach (Classification _val in _item.lstChild)
                                        {
                                            <li><a href="~/@_item.Parent.FriendlyURL/@_val.FriendlyURL">@_val.ClassificationNM</a></li>
                                        }
                                    </ul>
                                </div>
                            }
                        }

                        @foreach (SubMenuViewModels _item in Model)
                        {
                            if (_item.Parent.ClassificationId == (int)EnumCore.Classifi_Product_Cata.phan_thuoc)
                            {
                                <div class="col-sm-12 col-xs-6">
                                    <h2 class="thumb-headline">@_item.Parent.ClassificationNM</h2>
                                    <ul class="list-unstyled simple-list margin-bottom-20">
                                        @foreach (Classification _val in _item.lstChild)
                                        {
                                            <li><a href="~/@_item.Parent.FriendlyURL/@_val.FriendlyURL">@_val.ClassificationNM</a></li>
                                        }
                                    </ul>
                                </div>
                            }
                        }

                    </div>
                </div>




                <!-- End Simple List -->
            </div><!--/end row-->
        </div><!--/end continer-->
    </div><!--/footer-->

    <div class="copyright">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <p>
                        2014 &copy; Phong Lan Giá Sỉ. ALL Rights Reserved. Website được phát triển bởi
                        <a target="_blank" href="https://thuonghieuvang.net">Thương Hiệu Vàng</a>
                    </p>
                </div>
                <div class="col-md-6">

                    <ul class="list-inline shop-social pull-right">
                        <li><a href="#"><i class="fb rounded-md fa fa-facebook"></i></a></li>
                        <li><a href="#"><i class="tw rounded-md fa fa-twitter"></i></a></li>
                        <li><a href="#"><i class="gp rounded-md fa fa-google-plus"></i></a></li>
                        <li><a href="#"><i class="yt rounded-md fa fa-youtube"></i></a></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>